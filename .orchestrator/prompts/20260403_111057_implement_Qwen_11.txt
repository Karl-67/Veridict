A Claude subagent just implemented one file. Extract memory entries.

<task>[Attached image #1: C:/Users/PinkPanther/Downloads/WhatsApp%20Image%202026-04-03%20at%2010.47.22%20AM.jpeg]
(Use the Read tool to view the attached images above.)

i want you to look at the project, a</task>

<file_spec>
FILE: app/backend/models/schemas.py
SPEC: - Replace the flat upload/result schema set with the full `run-state-contract`.
- Add Pydantic models for `RunStatus`, `StageStatus`, `StageLease`, `EvidenceRef`, `ParsedClause`, `Finding`, `BranchReviewOutput`, `ValidatorOutput`, `AdminMergeOutput`, `AgreementDecision`, `FinalVerdict`, `RunSummary`
</file_spec>

<context>
CLAUDE OUTPUT (what the agent reported doing):
Done. Here's a summary of what was built and where the project stands:

---

## `schemas.py` — what changed

The flat prototype (5 models) is replaced with the full `run-state-contract`:

| Group | New models |
|---|---|
| **Enums/Literals** | `BranchName`, `IssueType`, `RecommendationType`, `ExploitabilityLevel`, `BusinessImpactLevel`, `ConsensusStatus`, `HumanAction`, `StageState`, `RunState`, `EventType`, `SeverityLevel` |
| **Parser** | `EvidenceRef`, `ParsedClause` |
| **Findings** | `Finding` |
| **Agent outputs** | `BranchReviewOutput`, `ValidatorOutput`, `AdminMergeOutput`, `AgreementDecision` |
| **Verdict** | `FinalVerdict` |
| **Run/Stage state** | `StageLease`, `StageStatus`, `RunStatus` |
| **API shapes** | `RunCreateResponse`, `RunSummary`, `RunDetail` |
| **SSE events** | `R
</context>

<output_format>
Return a JSON object (omit any key if nothing real to record):
{
  "key_facts": [{"category": "Code Conventions", "fact": "All handlers are async"}],
  "bugs": [{"issue": "...", "root_cause": "...", "solution": "...", "prevention": "..."}],
  "lessons": ["short concrete lesson"]
}
</output_format>

Only record things specific to THIS file and THIS task. No generic advice.
Return ONLY valid JSON, no markdown fences.