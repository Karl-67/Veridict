A Claude subagent just implemented one file. Extract memory entries.

<task>[Attached image #1: C:/Users/PinkPanther/Downloads/WhatsApp%20Image%202026-04-03%20at%2010.47.22%20AM.jpeg]
(Use the Read tool to view the attached images above.)

i want you to look at the project, a</task>

<file_spec>
FILE: app/backend/providers/base.py
SPEC: - Create the provider interface used by all role agents.
- Add `StructuredLLMProvider` with `generate_structured_output()` and raw request/response capture hooks.
- Define provider-level error types for invalid schema output, transient provider failure, rate limit, and non-retryable provider failure
</file_spec>

<context>
CLAUDE OUTPUT (what the agent reported doing):
`app/backend/providers/base.py` is created. Here's what's in it:

**Error hierarchy:**
- `ProviderError` — base
- `InvalidSchemaOutputError` — response doesn't conform to schema (non-retryable)
- `TransientProviderError` — 5xx / network timeout (retryable)
- `RateLimitError(TransientProviderError)` — 429, includes `retry_after_seconds`
- `NonRetryableProviderError` — auth failure, bad API key, policy rejection

**Capture dataclasses:**
- `RawProviderRequest` — snapshot of what was sent (model, prompt, schema, temperature, timestamp)
- `RawProviderResponse` — snapshot of what came back (raw text, parsed output, finish reason, usage, latency)

**`StructuredLLMProvider` abstract class:**
- `generate_structured_output(prompt, response_schema) -> dict` — the only method agents call
- `on_reques
</context>

<output_format>
Return a JSON object (omit any key if nothing real to record):
{
  "key_facts": [{"category": "Code Conventions", "fact": "All handlers are async"}],
  "bugs": [{"issue": "...", "root_cause": "...", "solution": "...", "prevention": "..."}],
  "lessons": ["short concrete lesson"]
}
</output_format>

Only record things specific to THIS file and THIS task. No generic advice.
Return ONLY valid JSON, no markdown fences.