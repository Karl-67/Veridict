<role>You are Codex, a senior technical lead (admin). Claude has just reviewed your proposal and added to it. Your job now is to critically scrutinize the combined plan before sign-off.</role>

<task>[Pasted text #1 — 47 lines]:
I will now send you the project rubric, that has everything required for the project, All of these should be met fully, and completely. I want you at the end to make a md file of the things that you can’t touch or make at this point in time, i want you to check the features, and check if the prompting is at it’s best for our use case. 
We should have the infrastructure for rag where you can just add (contracts/policies)pdfs, and it’s then added to the model knowledge base such that Harvey can actually check with them so they don’t have contracts that contradict with anything new or have policies that contradict with anything older. The contracts and policies that you can see at the moment are for training, we don’t have current contracts that will be used for rag, Harvey should have access to this rag information at the beginning so Harvey knows the context from the policies or contracts in addition to the contract the user uploads for review. 
Search for the Best platform for Deployment and put it in an md file, want to deploy a fine tuned gemma 4 26b models with 3 non fine tuned ones. everything you see within the system Harvey will have 3 other harveys to verify it’s work and kira the same. 
The problem we want to solve is to make an application for organizations that handle large contract volumes and want to reduce legal spend and review time. 
I also want you at the end to make an md file with every change made and to what extent do we currently.
We also want you to make a testing suit, of how to prove that fine tuned models were better then non fine tuned models, we want numbers, at the moment i don’t have finetuned models in the codebase, so you will make the pipeline with an interface so we can add the agents later on. 
Only Harvey will have access to rag. 
Harvey will check the context with other policies and give possible contradictions, and Kira will check if the sent (input) clauses and contracts have holes within them. 
The admin will make the consensus between the two outputs. 

Here is the whole project rubric which everything should be fully met. 
Live demo, public endpoint, end-to-end user flow.
Deployed API URL, cloud architecture, end-to-end behavior.
Architecture diagram, repo services, Docker services, API boundaries.
Repo, docs, deployment link, demo readiness, poster if required.
Project framing, problem statement or research framing, novelty/publishability evidence.
Architecture, repo, model/pipeline logic, deployed behavior.
Service boundary, code, container, endpoint contract, role in the system.
Service boundary, code, container, endpoint contract, role in the system.
External API code, request flow, orchestration logic, validation, request limits.
Tradeoffs section, benchmarks, experiments, measurements.
Demo behavior, outputs, logs, error cases, failure handling, test evidence.
Architecture docs, endpoint specs, service responsibilities, schemas.
Input schemas, payload constraints, validation code, limits/rate controls.
Error behavior, logs, timeout/retry policy, fallback behavior, docs.
Dockerfiles, image list, Docker Compose, Kubernetes manifests.
Deployment diagram, cloud setup, secrets strategy, cost notes.
Project statement, slides, docs, intro framing.
Baselines, comparisons, benchmark section, experiments.
Results, justification section, comparative evidence.
Business case, deployer/payer claim, target venue, paper draft, benchmark artifact.
Live presentation/demo.
Slides, verbal explanation, diagrams, walkthrough.
Metrics, benchmarks, tests, dashboards, tables, visuals.
Live answers, pacing, confidence, command of details.
Slides, poster, demo UI/UX, narrative impact.
Problem framing, system choice, comparison to common baselines/known demos.
Design decisions beyond minimum compliance, thoughtful constraints or clever system choices.
Repo tests, CI logs, demo of tests, coverage of unit/integration/E2E.
Golden datasets, regression checks, data validation, LLM evaluation logic, thresholds.
Commit history, authorship pattern, feature progression, issue linkage if any.
Branches, PRs/review notes, repo cleanliness, README, prompt version tracking if relevant.
Automation scripts, CI/CD, pipeline DAG, promotion/rollback logic.
MLflow/W&B/equivalent, metric history, thresholds, model/prompt selection logic.
Prometheus/Grafana/equivalent, dashboards, latency/error/throughput metrics, ML-specific signals.
Technical docs, business docs, runbook, tradeoffs section, endpoint docs, cost estimate.
Evidence of above-and-beyond work that is not already captured elsewhere.

this is everything i want you to know and do, do your best, my life depends on your output.

━━━ BACKGROUND RESEARCH (context only) ━━━
## Existing Solutions & Libraries

**Commercial comparables:** Harvey (legal AI workspace, document storage, due diligence, workflow agents), Kira (extraction-heavy contract review, provision models, Quick Study training), Ironclad (CLM integration, playbooks, clause detection, redlining), Luminance (enterprise contract intelligence, drafting support, portfolio insight). Gap: little public benchmark transparency, hard to know when AI is wrong, limited explainability beyond citations.

**Open-source/research:** PAKTON (multi-agent contract QA with RAG, similar architecture to Harvey/Kira/Admin); CUAD (510 contracts, 41 clause types, 13k+ labels); ContractNLI (contradiction/entailment reasoning); CLAUSE benchmark (tests subtle legal discrepancies).

**Tech stack:** Docling/PyMuPDF for extraction (pdfplumber currently used, should upgrade); LlamaIndex/LangChain/Haystack for chunking; pgvector/Qdrant for vector DB; bge-reranker/Cohere for reranking; vLLM/TGI for inference; Ragas/DeepEval for evaluation.

## Technical Approaches & Trade-offs

**RAG scope:** Harvey-only (no Kira RAG). Harvey retrieves from policy/contract PDFs; Kira focuses on internal holes in uploaded contract. Admin merges both. Hybrid retrieval (BM25 + embeddings) beats vector-only. Required metadata: `tenant_id`, `doc_type`, `policy_family_id`, `version`, `jurisdiction`, `source_path`, `page`, `chunk_hash`.

**Contradiction checking:** Same policy family only (safer, cheaper) vs. all-tenant search (catches more but increases false positives). Recommend phase 1 uses family + explicitly linked playbooks.

**Deployment:** Modal (Gemma + vLLM, H200 guidance, model cache volumes, public URL) > Runpod Serverless (cost, scale-to-zero) > HF Inference Endpoints (managed, expensive) > Koyeb. Suggested: Vercel frontend, Render/Fly/Modal CPU API, Neon/Supabase Postgres + pgvector, Modal vLLM for inference.

**Fine-tuned vs. baseline:** Identical prompts, retrieval context, decoding params. Metrics: JSON parse rate, contradiction F1/precision/recall, citation accuracy, hallucination rate, severity calibration, latency/cost. Use paired eval, bootstrap CI, McNemar test. Minimum claim: "fine-tuned beats base by X points on contradiction F1, reduces hallucinated citations by Y% on N examples."

## Architectural Patterns

**Current state:** 12-stage pipeline fully implemented end-to-end (create_run → parse_ocr_normalize → clause_index → harvey_context_load → kira_context_load → harvey_review_block → kira_review_block → admin_merge → final_review_block → awaiting_human_review → finalized). Multi-tenant auth, 3-reviewer branches (Harvey: issue_discovery/false_positive_challenge/exploitability_impact; Kira: same structure), admin deduplication, human review gate, vote aggregation with re-rounds, SSE event stream, exponential retry with jitter.

**Prompting issues:** No chain-of-thought elicitation; role-specific system prompts in user turn (not system role); schema hint lacks field descriptions; temperature 0.3 acceptable but validator should be 0.1; Harvey prompt missing RAG context integration.

**Test & monitoring gaps:** No repo tests exist (rubric requires unit/integration/E2E, golden datasets, LLM eval logic, thresholds); no Prometheus/Grafana; no MLflow/W&B experiment tracking; no CI/CD.

## Known Pitfalls & Challenges

- **No true RAG:** Currently Harvey loads only structured JSON from DB tables; PDF ingestion, chunking, embedding, retrieval pipeline entirely missing.
- **No fine-tuned weights:** Training pipeline complete; no trained model artifacts in codebase; all runs use base models.
- **No benchmark results:** No side-by-side evaluation runner; fine-tuning impact unproven.
- **No deployment:** Zero Docker, docker-compose, Kubernetes, cloud setup, public endpoint.
- **Parser quality:** pdfplumber first-pass; should upgrade to Docling for better evidence anchoring.
- **RISK-001:** Extraction confidence not gated downstream; low-confidence clauses not filtered before review.
- **RISK-002:** No ETL for CUAD/LEDGAR/MAUD parquets into ComplianceCorpus; corpus seeded manually.
- **RISK-008:** llama3.2:3b quality ceiling; coercion keeps pipeline running but cannot improve reasoning.</task>

<discussion>
EARLIER DISCUSSION (summary):
  [Codex]: Claude, proposed agreement:  - Treat current repo as partially implemented, not fully rubric-complete. The pasted “12-stage implemented” claim should...
  [Claude]: Codex, agreement with small additions:  **Confirmations** - Harvey-only RAG, Kira internal-holes only, Admin merge, final 3 reviewers gated through Ad...

RECENT DISCUSSION:
[Codex]: - The plan is outdated in places. `POST /api/runs`, `GET /api/runs/{id}`, SSE events, retry, findings, file download, and human review endpoints already exist in `app/backend/routes/contracts.py`. Treating these as missing will waste time and risks rewriting working code.

- The “Kira has no RAG” decision conflicts with current implementation. `kira_context_load` calls `resolve_applicable_corpora()` and injects internal/external compliance context into Kira. Either redefine “no RAG” as “no document-vector RAG” or remove Kira corpus loading. Current wording is internally inconsistent.

- The proposed RAG ingestion endpoint is underspecified. It says `POST /api/rag/documents`, but does not define auth dependency, tenant derivation, payload schema, file-size limits, duplicate handling, async ingestion status, failed chunk rollback, or version conflict behavior.

- “Admin-only” is hand-waved. Existing run creation accepts `tenant_id` as a form field. That is not tenant security. Before RAG ingestion, tenant identity must come from auth/org/workspace membership, not caller-provided form data.

- The citation rule is too blunt. “Empty citations means finding rejected” works for Harvey contradiction findings, but Kira findings should cite uploaded contract evidence, not RAG chunks. The schema needs separate `contract_evidence` and `rag_citations` or validators will reject valid Kira issues.

- “Retrieved RAG chunks injected BEFORE clause” is prompt folklore, not an architecture rule. The real requirement should be: Harvey gets a bounded, ranked, cited context block with retrieval trace IDs, and the validator checks citations against retrieved chunk IDs.

- Docling upgrade is not just a parser swap. Current parser is synchronous `pdfplumber` with fake bboxes and coarse confidence. Docling/OCR adds dependencies, runtime cost, model/package weight, deployment image impact, and different output structure. The plan does not account for that.

- The confidence gate is vague. Default `0.6` may block OCR-heavy PDFs and demo uploads. It needs per-page/per-clause handling, user-visible blocked state, override path, and tests around low-confidence evidence.

- The model ensemble mapping is still ambiguous. User asked for Harvey plus 3 verifier Harveys and Kira plus 3 verifier Kiras, plus 1 fine-tuned Gemma and 3 non-fine-tuned models. Claude maps this to existing 3 reviewer branches plus a model registry. That may not satisfy the user’s intended “multiple model verification” requirement.

- The eval matrix is under-scoped. If there are 3 Harvey branches, 3 Kira branches, 1 fine-tuned model, and 3 baselines, the plan must specify whether evaluation is per model, per branch, per role, or full pipeline. Otherwise metrics will be meaningless.

- Deployment recommendation is not sign-off ready. Modal/RunPod/HF pricing and GPU availability are volatile. We checked current docs/search results, but the final `DEPLOYMENT_RESEARCH.md` must cite live sources and include actual monthly demo/production cost estimates, cold-start impact, and VRAM feasibility for Gemma 4 26B A4B. Sources to verify: Modal docs/pricing, RunPod pricing, HF endpoint pricing, Google/HF Gemma model card.

- Docker scope is too large for Phase 1 if combined with RAG, Docling, eval, CI, Prometheus, Grafana, MLflow, Modal, admin UI, and public deployment. This is not a phase; it is several milestones.

- The plan ignores migration drift risk. Existing ORM and migrations already appear fragile. Adding pgvector/chunks/source documents/retrieval traces without first reconciling migrations can break local setup and deployment.

- Observability proposal is shallow. “Prometheus `/metrics`” is not enough. Need labels and exact metrics: run count by status, stage duration histogram, provider latency, provider failures, JSON repair count, retrieval hit count, citation validation failures, queue age, worker lease expiry.

- The frontend requirement is missing from the implementation plan. Acceptance says “RAG ingestion demonstrable via UI,” but touched files only mention backend/docs. Need admin upload page, ingestion status view, and retrieval trace/debug display.

- The docs list is excessive unless ownership and truth source are defined. `ARCHITECTURE.md`, `API_SPEC.md`, `RAG_DESIGN.md`, `RUNBOOK.md`, `DEPLOYMENT_RESEARCH.md`, `EVALUATION_PLAN.md`, `CURRENT_LIMITATIONS.md`, and `CHANGELOG_IMPLEMENTATION.md` will drift unless generated from schemas/config where possible.

- The plan does not define “can’t touch or make at this point in time” clearly. That requested artifact should be `docs/CURRENT_LIMITATIONS.md`, but it must distinguish impossible now, deferred by scope, blocked by missing assets, and blocked by deployment/secrets.

- The plan should not be signed off until it is split into executable milestones: stabilize existing run flow, reconcile DB/migrations, add Harvey-only RAG ingestion/retrieval, wire Harvey prompts/validation, add eval harness, then deployment/observability/docs.

DECLINED
[Codex]: Claude, revised agreement. This resolves the inconsistencies and makes Phase 1 executable.

- Do **not** rewrite the existing run API. Treat `app/backend/routes/contracts.py`, `run_service.py`, `event_stream.py`, `state_machine.py`, and `worker.py` as the current baseline. Developer should inspect and harden existing endpoints instead of recreating them.

- Define “Kira has no RAG” precisely:
  - Harvey gets document-vector RAG over tenant policies/playbooks/reference contracts.
  - Kira may use only uploaded-contract text and existing structured compliance/corpus logic if already wired, but must not retrieve tenant PDF chunks from the RAG index.
  - If product intent is strict “Kira sees uploaded contract only,” remove `kira_context_load` corpus injection.

- Phase 1 milestones:
  1. Stabilize existing run flow and tests.
  2. Reconcile ORM models, schemas, and Alembic migrations.
  3. Add Harvey-only RAG ingestion/retrieval.
  4. Wire Harvey prompt, citation validation, and retrieval traces.
  5. Add eval harness for baseline vs fine-tuned models.
  6. Add minimal deployment/docs/observability for demo.

- RAG endpoint:
  - `POST /api/rag/documents`
  - Admin/org-member auth required.
  - Tenant/workspace derived from auth, never from caller-provided form field.
  - Inputs: PDF file, `doc_type`, `policy_family_id`, `jurisdiction`, optional `version_label`.
  - Async ingestion job with status endpoint.
  - Duplicate detection by file hash and chunk hash.
  - Reingestion creates a new version and soft-deletes old active chunks.
  - Failed ingestion leaves prior active version intact.

- RAG storage:
  - Use Postgres + pgvector first.
  - Tables: source documents, document versions, chunks, embeddings, retrieval traces.
  - Required metadata: `tenant_id`, `doc_type`, `policy_family_id`, `version`, `jurisdiction`, `source_path`, `page`, `chunk_hash`.

- Harvey retrieval:
  - Filter by `tenant_id` and same `policy_family_id` plus explicitly linked playbooks/reference contracts.
  - Hybrid retrieval preferred: BM25/text search + vector search + optional rerank.
  - Prompt receives bounded ranked context with retrieval trace IDs.
  - Validator checks Harvey citations against retrieved chunk IDs.

- Citation schema:
  - Split evidence types.
  - `contract_evidence`: uploaded contract page/span/clause reference.
  - `rag_citations`: retrieved policy/playbook/reference-contract chunks.
  - Harvey contradiction findings require `rag_citations`.
  - Kira findings require `contract_evidence`, not RAG citations.

- Parser:
  - Docling becomes primary parser.
  - OCR fallback where needed.
  - Existing `pdfplumber` remains last fallback/config path.
  - Confidence must propagate to clauses/evidence.
  - Low-confidence handling must include visible blocked/warning state, override path, and tests. No blind global cutoff.

- Agents/prompts:
  - True system prompts where provider supports them.
  - Harvey prompt includes retrieved context, contradiction task, citation contract, and uncertainty flags.
  - Kira prompt focuses on drafting holes, ambiguity, missing protections, exploitability, and internal weaknesses.
  - No chain-of-thought request.
  - Require concise rationale, evidence spans, citations where applicable, and strict JSON.
  - Validator temperature `0.1`; reviewers `0.2`.

- Ensemble/model interpretation:
  - Product roles stay: Harvey, Kira, Admin, final reviewers.
  - Existing reviewer branches can remain: issue discovery, false-positive challenge, exploitability/impact.
  - Model registry supports 1 fine-tuned Gemma 26B and 3 non-fine-tuned baselines.
  - Eval must report per model, per role, and full-pipeline metrics so the “3 verifiers” and “4 model comparison” claims are not conflated.

- Evaluation:
  - Same prompts, retrieval context, decoding params, and fixtures across models.
  - Metrics: JSON validity, contradiction precision/recall/F1, citation accuracy, hallucinated citation rate, severity calibration, admin agreement, latency, cost.
  - Use paired eval, bootstrap confidence intervals, and McNemar for binary outcomes.
  - Missing fine-tuned endpoint is allowed: row marked `N/A - weights pending`, pipeline still runs.

- Deployment research doc:
  - Must verify live sources at implementation time.
  - Compare Modal, RunPod Serverless, Hugging Face Inference Endpoints, and at least one CPU/API hosting option.
  - Include VRAM feasibility for target Gemma 26B variant, cold-start behavior, public endpoint support, secrets, monthly demo estimate, and production estimate.
  - Recommendation remains likely: Vercel frontend, FastAPI on Render/Fly/Modal CPU, Supabase/Neon Postgres+pgvector, Modal vLLM for inference.

- Frontend:
  - Keep backend-driven run state and SSE.
  - Add admin-only RAG document upload/status UI.
  - Add retrieval trace/debug display for Harvey findings.
  - Human review panel must show Admin consensus and final reviewer state.

- Observability minimum:
  - FastAPI/worker `/metrics`.
  - Metrics: run count by status, stage duration histogram, provider latency/failures, JSON repair count, retrieval hit count, citation validation failures, queue age, retry count, worker lease expiry.
  - MLflow or W&B for eval runs only.

- Required docs:
  - `docs/CURRENT_LIMITATIONS.md`
  - `docs/CHANGELOG_IMPLEMENTATION.md`
  - `docs/DEPLOYMENT_RESEARCH.md`
  - `docs/EVALUATION_PLAN.md`
  - `docs/API_SPEC.md`
  - `docs/ARCHITECTURE.md`
  - `docs/RAG_DESIGN.md`
  - `docs/RUNBOOK.md`

- `CURRENT_LIMITATIONS.md` must categorize:
  - impossible now
  - blocked by missing model weights
  - blocked by missing secrets/deployment
  - deferred by scope
  - implemented but not benchmark-proven

- Phase 1 acceptance:
  - Existing run flow works end to end.
  - Harvey RAG ingestion/retrieval works with citations.
  - Kira does not use document-vector RAG.
  - Admin consensus and human review gate are visible.
  - Tests cover run lifecycle, retrieval filters, citation validation, parser confidence, and admin merge.
  - Eval runner compares configured models even with fine-tuned weights pending.
  - Deployment/docs are demo-ready, with limitations clearly stated.

AGREED
[Claude]: Codex, your revised plan resolves my concerns. Confirming alignment with two final clarifications:

**Locked decisions**
- Baseline = existing run flow; harden, don't rewrite.
- Kira = uploaded contract + existing structured corpus logic only; **no document-vector RAG**. Keep `kira_context_load` for structured corpus, but it must not query the new pgvector chunk index.
- Harvey-only RAG via pgvector; auth-derived tenant; split `contract_evidence` vs `rag_citations`; validator enforces citation→retrieval-trace match.
- 6 Phase-1 milestones in the order you listed.
- Model registry handles 1 FT Gemma 26B + 3 baselines; reviewer branches stay as-is; eval reports per-model, per-role, and full-pipeline.
- Docling primary, OCR fallback, pdfplumber last; confidence propagates with visible blocked state + override, no blind cutoff.
- Modal vLLM + Vercel + Neon/Supabase pgvector + Render/Fly CPU API; live-source verification at implementation time.
- 8 docs as listed; `CURRENT_LIMITATIONS.md` uses the 5 categories you defined.

**Two clarifications I'm adding**
- **Migration reconciliation (milestone 2) is a hard gate.** No new RAG tables until existing Alembic head is clean and `alembic upgrade head` succeeds on a fresh DB. Developer should produce a single new migration for the RAG schema, not stack on broken state.
- **Eval harness ships before deployment (milestone 5 before 6).** Even with FT row = "N/A pending," the harness + golden set + comparison table must exist so the demo can show the methodology — this is what the rubric's "baselines, comparisons, benchmark section" demands.

@User — one decision needed before developer starts:
**Strict-Kira or Pragmatic-Kira?**
- *Strict:* Kira sees ONLY uploaded contract text. Remove `kira_context_load` corpus injection entirely.
- *Pragmatic:* Kira keeps existing structured compliance corpus (CUAD/MAUD-derived rules from DB tables), but no PDF-vector RAG.

Codex and I default to **Pragmatic** unless you say otherwise — it preserves working code and matches "Kira finds holes" since the structured corpus encodes known clause weaknesses.

AGREED
[User]: btw i want to change the architecture such that for the harvey part where we currently have 3 reviewers and 1 harvey and 1 admin and 3 admin reviewers, i want to only have 3 harveys on one side so 3 reivewers, and to remove the admin revieweres.
</discussion>

<rules>
- Read Claude's additions carefully. Assume they may be wrong or incomplete.
- Actively hunt for: missing files, wrong paths, scope creep, bad architecture, vague specs, contradictions, anything that will cause the implementation to fail.
- Your DEFAULT stance is DECLINED. Only say AGREED if you genuinely cannot find a single gap or problem worth raising.
- If you have ANY concern — even minor — state it clearly and end with: DECLINED
- If the plan is airtight and you have zero objections, end with: AGREED
- Do NOT be polite. Do NOT rubber-stamp. Be the harshest reviewer on the team.
- No preamble. Bullet points only.
</rules>