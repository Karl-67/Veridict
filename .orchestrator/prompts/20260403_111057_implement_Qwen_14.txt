A Claude subagent just implemented one file. Extract memory entries.

<task>[Attached image #1: C:/Users/PinkPanther/Downloads/WhatsApp%20Image%202026-04-03%20at%2010.47.22%20AM.jpeg]
(Use the Read tool to view the attached images above.)

i want you to look at the project, a</task>

<file_spec>
FILE: app/backend/agents/reviewer.py
SPEC: - Replace the single `review_contract()` function with role-specific reviewer agents.
- Add `HarveyReviewerAgent`, `KiraReviewerAgent`, and `FinalReviewerAgent`.
- Add prompt builders that enforce reviewer differentiation: issue discovery, false-positive challenge, and exploitability/liability impac
</file_spec>

<context>
CLAUDE OUTPUT (what the agent reported doing):
The `reviewer.py` is now fully rewritten. Here's what changed and what the overall picture looks like:

---

**What was done:** `reviewer.py` now has three role-specific agent classes instead of the single `review_contract()` function:

| Class | Branch | Purpose |
|---|---|---|
| `HarveyReviewerAgent` | `harvey` | Policy-lineage review (internal playbooks, prior versions) |
| `KiraReviewerAgent` | `kira` | External compliance review (regulatory regimes, jurisdiction rules) |
| `FinalReviewerAgent` | cross-branch | Final review, up to 2 disagreement rounds |

Each takes a `reviewer_index` (1–3) that maps to a distinct prompt role:
- **1 → issue discovery** — surface everything, err on inclusion
- **2 → false-positive challenge** — keep only material, non-boilerplate issues
- **3 → exploita
</context>

<output_format>
Return a JSON object (omit any key if nothing real to record):
{
  "key_facts": [{"category": "Code Conventions", "fact": "All handlers are async"}],
  "bugs": [{"issue": "...", "root_cause": "...", "solution": "...", "prevention": "..."}],
  "lessons": ["short concrete lesson"]
}
</output_format>

Only record things specific to THIS file and THIS task. No generic advice.
Return ONLY valid JSON, no markdown fences.