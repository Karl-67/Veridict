A Claude subagent just implemented one file. Extract memory entries.

<task>[Attached image #1: C:/Users/PinkPanther/Downloads/WhatsApp%20Image%202026-04-03%20at%2010.47.22%20AM.jpeg]
(Use the Read tool to view the attached images above.)

i want you to look at the project, a</task>

<file_spec>
FILE: .orchestrator/key_facts.md
SPEC: - Record the locked stage names, run states, event names, Harvey lineage key, Kira corpus metadata, and canonical finding fields.
- Record the backend API contract and the required frontend SSE subscription behavior.
- Record that the current backend/frontend are scaffolding and not architecturally 
</file_spec>

<context>
CLAUDE OUTPUT (what the agent reported doing):
`key_facts.md` is now populated. Here's what's recorded and what it tells you about the gap between current state and target:

**What exists (scaffolding only):**
- Single Gemini call in `reviewer.py` — no stage graph
- Fake static pipeline stages in `contracts.py` — no real SSE, no run IDs
- Minimal schemas — no clause anchors, evidence, finding provenance

**What still needs to be built** (per the sibling contracts):
1. **DB layer** — `db/models.py` + `db/session.py` + Alembic migrations (Postgres stage queue)
2. **Config** — `core/config.py` with `Settings`
3. **Provider abstraction** — `providers/base.py` + `providers/google_provider.py` (Gemini behind the swappable boundary)
4. **Parser** — `services/parser.py` using Docling with OCR fallback + canonical clause index
5. **Agents** — `
</context>

<output_format>
Return a JSON object (omit any key if nothing real to record):
{
  "key_facts": [{"category": "Code Conventions", "fact": "All handlers are async"}],
  "bugs": [{"issue": "...", "root_cause": "...", "solution": "...", "prevention": "..."}],
  "lessons": ["short concrete lesson"]
}
</output_format>

Only record things specific to THIS file and THIS task. No generic advice.
Return ONLY valid JSON, no markdown fences.