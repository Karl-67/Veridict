A Claude subagent just implemented one file. Extract memory entries.

<task>[Attached image #1: C:/Users/PinkPanther/Downloads/WhatsApp%20Image%202026-04-03%20at%2010.47.22%20AM.jpeg]
(Use the Read tool to view the attached images above.)

i want you to look at the project, a</task>

<file_spec>
FILE: app/backend/providers/google_provider.py
SPEC: - Implement the Gemini-specific provider from `gemini-provider-boundary`.
- Use Gemini structured-output mode with explicit response schema binding instead of free-text JSON parsing.
- Implement request construction, retry/backoff, token-budget handling, response validation, and raw payload capture 
</file_spec>

<context>
CLAUDE OUTPUT (what the agent reported doing):
`app/backend/providers/google_provider.py` is created. Here's what it does:

**`GeminiProvider`** (extends `StructuredLLMProvider`):
- **Structured output**: every call binds `response_mime_type="application/json"` + `response_schema` so Gemini is constrained to emit schema-conformant JSON — no free-text parsing.
- **Retry/backoff**: up to `max_retries` attempts for `TransientProviderError` and `RateLimitError`, with exponential backoff + full jitter, capped at `max_backoff_seconds`. Respects `Retry-After` hints on 429s.
- **Token budget**: `max_output_tokens` is forwarded on every call via `GenerateContentConfig`.
- **Raw capture**: calls `on_request_captured` before each attempt and `on_response_captured` after success or terminal failure — both populated with `RawProviderRequest`/`RawPr
</context>

<output_format>
Return a JSON object (omit any key if nothing real to record):
{
  "key_facts": [{"category": "Code Conventions", "fact": "All handlers are async"}],
  "bugs": [{"issue": "...", "root_cause": "...", "solution": "...", "prevention": "..."}],
  "lessons": ["short concrete lesson"]
}
</output_format>

Only record things specific to THIS file and THIS task. No generic advice.
Return ONLY valid JSON, no markdown fences.