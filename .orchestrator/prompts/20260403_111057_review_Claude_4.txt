<role>You are a senior technical lead doing a secondary code review.
Codex (another lead) reviewed the diff and flagged issues.
Your job: validate each finding against the actual diff.</role>

<rules>
- VALID: actual bugs, logic errors, plan deviations visible in the diff.
- INVALID: requests for tests, docs, comments, style, or anything not
  visible in the diff below.
- Reject hallucinated issues (issues Codex claims exist but the diff
  shows are already handled or don't exist at all).
</rules>

<task>[Attached image #1: C:/Users/PinkPanther/Downloads/WhatsApp%20Image%202026-04-03%20at%2010.47.22%20AM.jpeg]
(Use the Read tool to view the attached images above.)

i want you to look at the project, and check what is still required to finalize this architecture, the current schema should be calling a google api, we can continue with that for now however, i would like to make it later be a local agent that does all of this that we should be fine tuning none of this is current there but its the plan for the final product. if you have any questions for clarification ask them. i want this to be super clear for you so you can apply what i want exactly.

━━━ BACKGROUND RESEARCH (context only) ━━━
I don't have permission to view the image yet. However, I can work from the three detailed analyses already provided. Let me aggregate those findings into your briefing:

---

## Existing Solutions & Libraries

**Document processing:**
- Docling for structured conversion with OCR support; Unstructured for partitioning; Marker for PDF→markdown/JSON.
- `pdfplumber` is insufficient for serious legal workflows (scanned PDFs, tables, multi-column layouts fail silently).

**Orchestration & state:**
- LangGraph for graph-based stateful workflows with branching; Temporal for resumable long-running jobs + human review steps; PydanticAI for typed outputs.
- FastAPI background tasks adequate for light work only; move to proper job queue for production.

**Model serving (future local path):**
- vLLM or Ollama for inference; TRL/Axolotl/LLaMA-Factory for fine-tuning; Outlines for schema-constrained decoding.

**Storage & retrieval:**
- Postgres + SQLAlchemy for contracts, outputs, audit data.
- Vector store (pgvector, Qdrant) only if precedent/policy retrieval is semantic, not version lookup.

---

## Technical Approaches & Trade-offs

**Immediate fixes for Google API path:**
- Use Google's structured output (JSON schema) instead of parsing free-form text.
- Add provider abstraction layer now to decouple Gemini from business logic.
- Replace single giant contract prompt with clause extraction + targeted review passes.
- Add OCR fallback for scanned PDFs; current text truncation silently drops content.

**Phased migration to local agents:**
- Phase 1: Keep Gemini, add real orchestration + strict schemas + evidence spans.
- Phase 2: Add policy store, precedent retrieval, disagreement logic, admin merge.
- Phase 3: Log corrections to build fine-tuning dataset.
- Phase 4: Fine-tune narrow subtasks first (clause classification, risk labeling, disagreement arbitration), not whole architecture.
- Phase 5: Swap Gemini reviewers one-by-one behind same interface.

**Hybrid approach preferred:** Local model for reasoning + deterministic rules for policy checks + retrieval for lookup + optional external fallback.

---

## Architectural Patterns

**Current gap:** Single Gemini call returns one JSON; frontend simulates pipeline with timers; backend hard-codes stage names regardless of actual execution.

**Required structure:**
- Explicit execution graph: `ingest → normalize/chunk → Harvey branch → Kira branch → 3 reviewers per branch → validators → admin adjudicator → verdict`.
- Separate agent roles: `harvey_conflict_detector`, `kira_internal_policy_checker`, `reviewer`, `validator`, `admin` with distinct prompts/schemas.
- Shared typed state object across stages tracking: document ID, policy ID, clauses, evidence spans, findings, branch verdicts, consensus score, disagreements.
- Per-branch output schemas (Harvey ≠ Kira).
- Evidence grounding: every finding includes source span, page, confidence, rationale.
- Job/run IDs with `202 Accepted` responses; persist state; stream SSE/WebSocket progress to UI.
- Retry/idempotency per stage; handle parse errors, rate limits, partial outputs.

---

## Known Pitfalls & Challenges

**Definition gaps:**
- "Previous policy conflict" undefined: direct contradiction vs. scope mismatch vs. version regression vs. exception mismatch vs. approval workflow mismatch.
- "Harvey branch" scope unclear: check only prior versions of same policy, or all organizational policies, or curated subset?
- "Kira branch" scope unclear: internal policy only, or broader compliance/risk engine?

**Multi-reviewer design risk:** Three reviewers seeing the same draft context can self-reinforce hallucinations. Better: independent first-pass, blinded validator, admin sees only structured diffs + evidence.

**Document handling:** Tables, appendices, cross-references, defined terms require clause-level extraction that preserves section numbers. Long documents need clause indexing; naive context-window usage will fail.

**Retrieval risks:** Embeddings can return wrong-tenant policies unless tenant filters enforced before vector search. Consensus is not accuracy.

**PDF text extraction:** Scanned PDFs and complex layouts break standard extraction.

---

## Critical Unknowns Blocking Full Architecture

- Are the 3 reviewers intended as independent parallel passes with different prompts, or sequential critique/revision?
- Final local product: one fine-tuned model for all roles, or multiple role-specific models?
- Should final output include clause-level redlines + fallback language, or issue spotting + recommendations only?</task>

<plan>────────────────────────
## Shared Dependencies
`phase-1-architecture-spec`: authoritative phase-1 architecture using Gemini now and provider-swappable local agents later; locks stage graph, review-loop rules, human approval gate, and system boundaries (used by: orchestrator.md, .orchestrator/decisions.md, .orchestrator/key_facts.md, .orchestrator/issues.md, app/README.md)

`run-state-contract`: canonical typed contract for runs, stages, findings, evidence anchors, human-review actions, events, and final verdict payloads (used by: app/backend/models/schemas.py, app/backend/routes/contracts.py, app/backend/agents/reviewer.py, app/backend/agents/validator.py, app/backend/agents/admin.py, app/backend/services/run_service.py, app/backend/services/event_stream.py, app/backend/orchestration/state_machine.py, app/frontend/src/types/index.ts, app/frontend/src/lib/api.ts, app/frontend/src/App.tsx, app/frontend/src/components/PipelineTracker.tsx, app/frontend/src/components/VerdictCard.tsx, app/frontend/src/components/HumanReviewPanel.tsx)

`gemini-provider-boundary`: provider abstraction where Gemini owns transport, structured-output schema binding, retry/backoff, and raw response capture; role agents own prompts and interpretation so local models can replace Gemini later (used by: app/backend/core/config.py, app/backend/providers/base.py, app/backend/providers/google_provider.py, app/backend/agents/reviewer.py, app/backend/agents/validator.py, app/backend/agents/admin.py, app/backend/requirements.txt, app/README.md)

`docling-canonical-parser`: Docling-first PDF parsing with OCR fallback and canonical clause anchors including document hash, parser version, clause UID, page, bbox, normalized text, and extraction confidence (used by: orchestrator.md, app/backend/models/schemas.py, app/backend/services/parser.py, app/backend/db/models.py, app/backend/orchestration/state_machine.py, app/backend/requirements.txt)

`policy-lineage-and-corpora`: Harvey lineage keyed by `tenant_id </plan>

<diff_summary>
FILES CHANGED: 18

AGENTS.md (+15 -0):
  (configuration/data changes)

CLAUDE.md (+15 -0):
  (configuration/data changes)

app/backend/agents/reviewer.py (+512 -43):
  Added: FinalReviewerAgent
  Added: HarveyReviewerAgent
  Added: KiraReviewerAgent
  Added: __init__
  Added: _assemble_branch_output
  Added: _build_final_prompt
  Added: _build_harvey_prompt
  Added: _build_kira_prompt
  Added: _format_clauses
  Added: _format_compliance_context
  Added: _format_merged_findings
  Added: _format_policy_context
  Added: agent_role
  Added: review

app/backend/agents/validator.py (+352 -0):
  Added: HarveyValidatorAgent
  Added: KiraValidatorAgent
  Added: __init__
  Added: _build_validator_prompt
  Added: _deduplicate_overlapping_findings
  Added: _normalize_reviewer_outputs
  Added: _quality
  Added: _score_evidence_quality
  Added: validate

app/backend/alembic/__init__.py (+0 -0):

app/backend/alembic/env.py (+142 -0):
  Added: _get_db_url
  Added: run_migrations_offline
  Added: run_migrations_online

app/backend/alembic/versions/0001_initial_architecture.py (+276 -0):
  Added: downgrade
  Added: upgrade

app/backend/alembic/versions/__init__.py (+0 -0):

app/backend/core/config.py (+47 -0):
  Added: Settings
  Added: get_settings

app/backend/db/models.py (+460 -0):
  Added: Base
  Added: ComplianceCorpusRecord
  Added: EvidenceRecord
  Added: FindingRecord
  Added: HumanReviewRecord
  Added: ParsedClauseRecord
  Added: PolicyVersionRecord
  Added: RunEventRecord
  Added: RunRecord
  Added: StageExecutionRecord
  Added: _now
  Added: _uuid

app/backend/db/session.py (+165 -0):
  Added: _async_dsn
  Added: _sync_dsn
  Added: get_engine
  Added: get_session_factory
  Added: get_sync_db_session
  Added: get_sync_engine
  Added: get_sync_session_factory

app/backend/models/schemas.py (+375 -2):
  Added: AdminMergeOutput
  Added: AgreementDecision
  Added: BranchReviewOutput
  Added: EvidenceRef
  Added: FinalVerdict
  Added: Finding
  Added: HumanReviewAction
  Added: HumanReviewPayload
  Added: HumanReviewResult
  Added: ParsedClause
  Added: RunCreateResponse
  Added: RunDetail
  Added: RunEvent
  Added: RunStatus
  Added: RunSummary
  Added: StageLease
  Added: StageStatus
  Added: ValidatorOutput

app/backend/providers/__init__.py (+0 -0):

app/backend/providers/base.py (+196 -0):
  Added: InvalidSchemaOutputError
  Added: NonRetryableProviderError
  Added: ProviderError
  Added: RateLimitError
  Added: RawProviderRequest
  Added: RawProviderResponse
  Added: StructuredLLMProvider
  Added: TransientProviderError
  Added: __init__
  Added: on_request_captured
  Added: on_response_captured

app/backend/providers/google_provider.py (+362 -0):
  Added: GeminiProvider
  Added: __init__
  Added: _backoff_delay
  Added: _classify_genai_error
  Added: _extract_finish_reason
  Added: _extract_status_code
  Added: _extract_usage
  Added: _parse_structured_response
  Added: build_gemini_provider

app/backend/requirements.txt (+29 -7):
  (configuration/data changes)

app/backend/services/policy_repository.py (+164 -0):
  Added: MissingLineageError
  Added: __init__
  Added: _record_to_dict
  Added: _session
  Added: load_prior_policy_versions
  Added: resolve_policy_lineage

orchestrator.md (+370 -0):
  (configuration/data changes)
</diff_summary>

<review_findings>
CHANGES_REQUESTED

1. `run-state-contract` was not carried through the runtime boundary. The plan explicitly lists [app/backend/routes/contracts.py](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/backend/routes/contracts.py), [app/backend/services/run_service.py](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/backend/services/run_service.py), [app/backend/services/event_stream.py](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/backend/services/event_stream.py), and [app/backend/orchestration/state_machine.py](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/backend/orchestration/state_machine.py) as consumers of the canonical run contract, but none of them appear in the diff. That is a direct deviation from the architecture plan: the new schemas exist, but the execution path that creates runs, advances stages, persists events, and exposes progress was not updated with them.

2. The frontend contract migration is incomplete. The plan explicitly requires [app/frontend/src/types/index.ts](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/frontend/src/types/index.ts), [app/frontend/src/lib/api.ts](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/frontend/src/lib/api.ts), [app/frontend/src/App.tsx](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/frontend/src/App.tsx), [app/frontend/src/components/PipelineTracker.tsx](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/frontend/src/components/PipelineTracker.tsx), [app/frontend/src/components/VerdictCard.tsx](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/frontend/src/components/VerdictCard.tsx), and [app/frontend/src/components/HumanReviewPanel.tsx](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/frontend/src/components/HumanReviewPanel.tsx) to adopt the canonical run-state contract. None of those files changed, so the UI cannot be considered aligned with the finalized architecture.

3. The document-ingestion phase is only partially implemented relative to plan. `docling-canonical-parser` is supposed to land in [app/backend/services/parser.py](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/backend/services/parser.py) and [app/backend/orchestration/state_machine.py](/C:/Users/PinkPanther/PycharmProjects/Verdict/app/backend/orchestration/state_machine.py), but neither file appears in the diff. Adding `ParsedClause` and DB tables without the parser/orchestrator changes means the required Docling-first parsing + OCR fallback + canonical clause anchor flow is still missing from execution.
</review_findings>

<output_format>
If you need the full diff for specific files to validate a finding,
respond with NEED_FULL_DIFF: filename1, filename2

Otherwise, your response MUST start with exactly one of:
CONFIRMED — at least one issue is valid
APPROVED  — all issues are invalid, implementation is correct

If CONFIRMED, list ONLY the confirmed issues as numbered bullets.
One sentence each. Actionable. No new issues beyond what Codex flagged.
</output_format>