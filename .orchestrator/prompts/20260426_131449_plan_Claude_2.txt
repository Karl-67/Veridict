<role>You are Claude, a senior technical lead. Codex has drafted an implementation plan. Read it carefully and produce the final improved plan.</role>

<task>[Pasted text #1 — 47 lines]:
I will now send you the project rubric, that has everything required for the project, All of these should be met fully, and completely. I want you at the end to make a md file of the things that you can’t touch or make at this point in time, i want you to check the features, and check if the prompting is at it’s best for our use case. 
We should have the infrastructure for rag where you can just add (contracts/policies)pdfs, and it’s then added to the model knowledge base such that Harvey can actually check with them so they don’t have contracts that contradict with anything new or have policies that contradict with anything older. The contracts and policies that you can see at the moment are for training, we don’t have current contracts that will be used for rag, Harvey should have access to this rag information at the beginning so Harvey knows the context from the policies or contracts in addition to the contract the user uploads for review. 
Search for the Best platform for Deployment and put it in an md file, want to deploy a fine tuned gemma 4 26b models with 3 non fine tuned ones. everything you see within the system Harvey will have 3 other harveys to verify it’s work and kira the same. 
The problem we want to solve is to make an application for organizations that handle large contract volumes and want to reduce legal spend and review time. 
I also want you at the end to make an md file with every change made and to what extent do we currently.
We also want you to make a testing suit, of how to prove that fine tuned models were better then non fine tuned models, we want numbers, at the moment i don’t have finetuned models in the codebase, so you will make the pipeline with an interface so we can add the agents later on. 
Only Harvey will have access to rag. 
Harvey will check the context with other policies and give possible contradictions, and Kira will check if the sent (input) clauses and contracts have holes within them. 
The admin will make the consensus between the two outputs. 

Here is the whole project rubric which everything should be fully met. 
Live demo, public endpoint, end-to-end user flow.
Deployed API URL, cloud architecture, end-to-end behavior.
Architecture diagram, repo services, Docker services, API boundaries.
Repo, docs, deployment link, demo readiness, poster if required.
Project framing, problem statement or research framing, novelty/publishability evidence.
Architecture, repo, model/pipeline logic, deployed behavior.
Service boundary, code, container, endpoint contract, role in the system.
Service boundary, code, container, endpoint contract, role in the system.
External API code, request flow, orchestration logic, validation, request limits.
Tradeoffs section, benchmarks, experiments, measurements.
Demo behavior, outputs, logs, error cases, failure handling, test evidence.
Architecture docs, endpoint specs, service responsibilities, schemas.
Input schemas, payload constraints, validation code, limits/rate controls.
Error behavior, logs, timeout/retry policy, fallback behavior, docs.
Dockerfiles, image list, Docker Compose, Kubernetes manifests.
Deployment diagram, cloud setup, secrets strategy, cost notes.
Project statement, slides, docs, intro framing.
Baselines, comparisons, benchmark section, experiments.
Results, justification section, comparative evidence.
Business case, deployer/payer claim, target venue, paper draft, benchmark artifact.
Live presentation/demo.
Slides, verbal explanation, diagrams, walkthrough.
Metrics, benchmarks, tests, dashboards, tables, visuals.
Live answers, pacing, confidence, command of details.
Slides, poster, demo UI/UX, narrative impact.
Problem framing, system choice, comparison to common baselines/known demos.
Design decisions beyond minimum compliance, thoughtful constraints or clever system choices.
Repo tests, CI logs, demo of tests, coverage of unit/integration/E2E.
Golden datasets, regression checks, data validation, LLM evaluation logic, thresholds.
Commit history, authorship pattern, feature progression, issue linkage if any.
Branches, PRs/review notes, repo cleanliness, README, prompt version tracking if relevant.
Automation scripts, CI/CD, pipeline DAG, promotion/rollback logic.
MLflow/W&B/equivalent, metric history, thresholds, model/prompt selection logic.
Prometheus/Grafana/equivalent, dashboards, latency/error/throughput metrics, ML-specific signals.
Technical docs, business docs, runbook, tradeoffs section, endpoint docs, cost estimate.
Evidence of above-and-beyond work that is not already captured elsewhere.

this is everything i want you to know and do, do your best, my life depends on your output.

━━━ BACKGROUND RESEARCH (context only) ━━━
## Existing Solutions & Libraries

**Commercial comparables:** Harvey (legal AI workspace, document storage, due diligence, workflow agents), Kira (extraction-heavy contract review, provision models, Quick Study training), Ironclad (CLM integration, playbooks, clause detection, redlining), Luminance (enterprise contract intelligence, drafting support, portfolio insight). Gap: little public benchmark transparency, hard to know when AI is wrong, limited explainability beyond citations.

**Open-source/research:** PAKTON (multi-agent contract QA with RAG, similar architecture to Harvey/Kira/Admin); CUAD (510 contracts, 41 clause types, 13k+ labels); ContractNLI (contradiction/entailment reasoning); CLAUSE benchmark (tests subtle legal discrepancies).

**Tech stack:** Docling/PyMuPDF for extraction (pdfplumber currently used, should upgrade); LlamaIndex/LangChain/Haystack for chunking; pgvector/Qdrant for vector DB; bge-reranker/Cohere for reranking; vLLM/TGI for inference; Ragas/DeepEval for evaluation.

## Technical Approaches & Trade-offs

**RAG scope:** Harvey-only (no Kira RAG). Harvey retrieves from policy/contract PDFs; Kira focuses on internal holes in uploaded contract. Admin merges both. Hybrid retrieval (BM25 + embeddings) beats vector-only. Required metadata: `tenant_id`, `doc_type`, `policy_family_id`, `version`, `jurisdiction`, `source_path`, `page`, `chunk_hash`.

**Contradiction checking:** Same policy family only (safer, cheaper) vs. all-tenant search (catches more but increases false positives). Recommend phase 1 uses family + explicitly linked playbooks.

**Deployment:** Modal (Gemma + vLLM, H200 guidance, model cache volumes, public URL) > Runpod Serverless (cost, scale-to-zero) > HF Inference Endpoints (managed, expensive) > Koyeb. Suggested: Vercel frontend, Render/Fly/Modal CPU API, Neon/Supabase Postgres + pgvector, Modal vLLM for inference.

**Fine-tuned vs. baseline:** Identical prompts, retrieval context, decoding params. Metrics: JSON parse rate, contradiction F1/precision/recall, citation accuracy, hallucination rate, severity calibration, latency/cost. Use paired eval, bootstrap CI, McNemar test. Minimum claim: "fine-tuned beats base by X points on contradiction F1, reduces hallucinated citations by Y% on N examples."

## Architectural Patterns

**Current state:** 12-stage pipeline fully implemented end-to-end (create_run → parse_ocr_normalize → clause_index → harvey_context_load → kira_context_load → harvey_review_block → kira_review_block → admin_merge → final_review_block → awaiting_human_review → finalized). Multi-tenant auth, 3-reviewer branches (Harvey: issue_discovery/false_positive_challenge/exploitability_impact; Kira: same structure), admin deduplication, human review gate, vote aggregation with re-rounds, SSE event stream, exponential retry with jitter.

**Prompting issues:** No chain-of-thought elicitation; role-specific system prompts in user turn (not system role); schema hint lacks field descriptions; temperature 0.3 acceptable but validator should be 0.1; Harvey prompt missing RAG context integration.

**Test & monitoring gaps:** No repo tests exist (rubric requires unit/integration/E2E, golden datasets, LLM eval logic, thresholds); no Prometheus/Grafana; no MLflow/W&B experiment tracking; no CI/CD.

## Known Pitfalls & Challenges

- **No true RAG:** Currently Harvey loads only structured JSON from DB tables; PDF ingestion, chunking, embedding, retrieval pipeline entirely missing.
- **No fine-tuned weights:** Training pipeline complete; no trained model artifacts in codebase; all runs use base models.
- **No benchmark results:** No side-by-side evaluation runner; fine-tuning impact unproven.
- **No deployment:** Zero Docker, docker-compose, Kubernetes, cloud setup, public endpoint.
- **Parser quality:** pdfplumber first-pass; should upgrade to Docling for better evidence anchoring.
- **RISK-001:** Extraction confidence not gated downstream; low-confidence clauses not filtered before review.
- **RISK-002:** No ETL for CUAD/LEDGAR/MAUD parquets into ComplianceCorpus; corpus seeded manually.
- **RISK-008:** llama3.2:3b quality ceiling; coercion keeps pipeline running but cannot improve reasoning.</task>

<codex_plan>
────────────────────────
## Shared Dependencies
`stage-topology`: canonical pipeline stages are `create_run`, `parse_ocr_normalize`, `clause_index`, `harvey_context_load`, `kira_context_load`, `harvey_review_block`, `kira_review_block`, `admin_merge`, `awaiting_human_review`, `finalized`; `final_review_block` and admin reviewer stages are removed/disabled (used by: app/backend/services/run_service.py, app/backend/orchestration/state_machine.py, app/frontend/src/components/PipelineTracker.tsx, docs/ARCHITECTURE.md)

`harvey-trio-contract`: Harvey side has exactly 3 Harvey reviewers/agents producing independent findings or votes; Admin remains only as merge/consensus agent; no admin reviewers/final reviewers exist in production flow (used by: app/backend/agents/reviewer.py, app/backend/agents/admin.py, app/backend/orchestration/state_machine.py, app/backend/services/run_service.py, app/frontend/src/components/VerdictCard.tsx, docs/EVALUATION_PLAN.md)

`harvey-rag-only`: only Harvey can query tenant PDF/vector RAG; Kira cannot query document-vector RAG and may only use uploaded contract text plus existing structured corpus logic unless explicitly removed later (used by: app/backend/services/rag_retrieval.py, app/backend/orchestration/state_machine.py, app/backend/agents/reviewer.py, docs/RAG_DESIGN.md)

`evidence-schema`: split evidence into `contract_evidence` and `rag_citations`; Harvey contradiction findings require validated `rag_citations`, Kira findings require `contract_evidence`; validators check cited IDs against retrieval traces (used by: app/backend/models/schemas.py, app/backend/db/models.py, app/backend/agents/validator.py, app/frontend/src/types/index.ts, app/frontend/src/components/VerdictCard.tsx)

`auth-derived-tenancy`: tenant/workspace identity must come from authenticated membership, never caller-provided form fields; RAG ingestion is admin/workspace-admin only (used by: app/backend/routes/rag.py, app/backend/services/auth_service.py, app/backend/routes/contracts.py)

`rag-storage-contract`: Postgres + pgvector storage for source documents, document versions, chunks, embeddings, ingestion jobs, and retrieval traces; metadata includes `tenant_id`, `doc_type`, `policy_family_id`, `version`, `jurisdiction`, `source_path`, `page`, `chunk_hash` (used by: app/backend/db/models.py, app/backend/alembic/versions/0003_add_harvey_rag.py, app/backend/services/rag_ingestion.py, app/backend/services/rag_retrieval.py)

`parser-confidence-contract`: Docling primary parser, OCR fallback, pdfplumber last fallback; confidence propagates to pages, clauses, and evidence anchors with visible warning/blocked/override states, not a blind global cutoff (used by: app/backend/services/parser.py, app/backend/orchestration/state_machine.py, app/frontend/src/components/PipelineTracker.tsx, tests/test_parser_confidence.py)

`model-registry-contract`: model registry supports 1 fine-tuned Gemma 26B endpoint and 3 non-fine-tuned baseline endpoints; disabled/missing endpoints are reported as `N/A - weights pending` instead of crashing (used by: configs/models.yaml, scripts/eval_model_registry.py, scripts/eval_finetune_vs_baseline.py, docs/EVALUATION_PLAN.md)

`observability-contract`: metrics include run count by status, stage duration histogram, provider latency/failures, JSON repair count, retrieval hit count, citation validation failures, queue age, retry count, and worker lease expiry (used by: app/backend/main.py, app/backend/worker.py, app/backend/services/metrics.py, docs/RUNBOOK.md)

## Files

### app/backend/services/run_service.py (MODIFY)
- Remove `final_review_block` from `STAGE_SEQUENCE`.
- Ensure created runs use `stage-topology` exactly.
- Update `_load_full_findings()` so source priority no longer references `final_reviewer`; valid sources are `admin`, `harvey`, and `kira`.
- Update `_build_final_verdict()` to expose Harvey trio consensus, Kira findings, Admin merged findings, human action, unresolved consensus flags, and evidence fields from `evidence-schema`.
- Update `get_run_detail()` so returned stage list never includes removed final/admin reviewer stages for new runs.
- Update `submit_human_review()` and `finalize_run_if_approved()` to finalize directly after Admin merge plus human review.
- Add migration-safe handling for legacy runs that still contain `final_review_block`: display as legacy-only but do not enqueue or require it.

### app/backend/orchestration/state_machine.py (MODIFY)
- Remove import and execution path for `FinalReviewerAgent`.
- Remove `_enqueue_final_review_stage()` or change it to a no-op for legacy compatibility only.
- Update `execute_stage()` so the active graph follows `stage-topology`.
- Update `harvey_review_block` execution to run exactly 3 Harvey reviewers using `harvey-trio-contract`.
- Update `admin_merge` execution so Admin merges Harvey trio consensus with Kira output and does not call final/admin reviewers.
- Keep `kira_context_load` from calling document-vector RAG; enforce `harvey-rag-only`.
- Add retrieval trace handoff from `harvey_context_load` into `harvey_review_block`.
- Fix `_stage_output()` to order by latest `round_number`, `attempt_number`, and `finished_at` before selecting output, closing BUG-012.
- Add blocked/warning state behavior for low-confidence parser outputs per `parser-confidence-contract`.
- Ensure SSE events never emit `final_review_block` for new runs.

### app/backend/agents/reviewer.py (MODIFY)
- Remove or deprecate `FinalReviewerAgent` from active runtime.
- Define the 3 Harvey reviewer identities under `harvey-trio-contract`: `harvey_issue_discovery`, `harvey_false_positive_challenge`, `harvey_exploitability_impact`.
- Ensure each Harvey reviewer receives uploaded contract evidence plus bounded ranked RAG context from `harvey-rag-only`.
- Add Harvey prompt requirements: contradiction task, policy/playbook/reference-contract citation contract, uncertainty flags, concise rationale, strict JSON, no chain-of-thought request.
- Ensure Kira prompt cannot access document-vector RAG and focuses on drafting holes, ambiguity, missing protections, exploitability, and internal weaknesses.
- Move role instructions into provider system prompts where supported.
- Set reviewer temperature default to `0.2`.
- Add output fields required by `evidence-schema`: `contract_evidence`, `rag_citations`, `consensus_state`, `business_impact`, `exploitability`, `unresolved_by_consensus`, and human review placeholders.

### app/backend/agents/admin.py (MODIFY)
- Keep Admin as the single merge/consensus layer.
- Remove active concepts of admin reviewers/final reviewers.
- Update `ConsensusAdmin.merge()` to accept Harvey trio output plus Kira output.
- Update `AgreementAdmin.check()` to compute Harvey 1/2/3 agreement and unresolved consensus status.
- Update `_aggregate_from_reviewers()` and `resolve_votes()` terminology from generic final reviewers to Harvey trio reviewers.
- Emit Admin output with merged findings, deduped issue groups, consensus state, unresolved items, and evidence references from `evidence-schema`.

### app/backend/agents/validator.py (MODIFY)
- Fix inconsistent import paths.
- Set validator provider temperature to `0.1`.
- Add validation for `contract_evidence` and `rag_citations`.
- Reject Harvey contradiction findings missing valid `rag_citations`.
- Reject Kira findings missing uploaded-contract `contract_evidence`.
- Validate Harvey `rag_citations` against retrieval trace chunk IDs.
- Validate no Kira finding contains document-vector RAG citation IDs.
- Return explicit validation errors for citation mismatch, missing evidence, malformed JSON, and unsupported source agent.

### app/backend/models/schemas.py (MODIFY)
- Add Pydantic models: `ContractEvidence`, `RagCitation`, `RetrievalTraceItem`, `HarveyReviewerOutput`, `HarveyTrioConsensus`, `KiraReviewerOutput`, `AdminConsensusOutput`, `RagDocumentCreateResponse`, `RagDocumentStatus`, `RagDocumentListItem`, `ModelRegistryEntry`, `EvalMetricRow`.
- Modify `Finding`, `ReviewResult`, `FinalVerdict`, and run detail schemas to include branch grouping, consensus state, evidence anchors, exploitability, business impact, unresolved_by_consensus, and human review fields.
- Remove active references to `final_reviewer` from response schemas.
- Keep backward-compatible optional fields for legacy persisted runs.
- Add request/response schemas for RAG endpoints listed in `app/backend/routes/rag.py`.

### app/backend/db/models.py (MODIFY)
- Add ORM models for `RagSourceDocument`, `RagDocumentVersion`, `RagChunk`, `RagEmbedding`, `RagIngestionJob`, and `RagRetrievalTrace`.
- Add columns to `FindingRecord` or JSON structure support for `contract_evidence`, `rag_citations`, `consensus_state`, `business_impact`, `exploitability`, and `unresolved_by_consensus`.
- Add indexes for tenant/workspace filtering, policy family lookup, active document versions, chunk hash duplicate detection, and retrieval trace lookup.
- Add pgvector embedding column on `RagEmbedding`.
- Update comments/enums so active stage names match `stage-topology`.
- Preserve legacy stage rows without requiring them in new runs.

### app/backend/alembic/versions/0003_add_harvey_rag.py (CREATE)
- Create pgvector extension if available.
- Create `rag_source_documents`.
- Create `rag_document_versions`.
- Create `rag_chunks`.
- Create `rag_embeddings`.
- Create `rag_ingestion_jobs`.
- Create `rag_retrieval_traces`.
- Add indexes and uniqueness constraints for `tenant_id`, `workspace_id`, `policy_family_id`, `document_id`, `version`, `chunk_hash`, and active version lookup.
- Add nullable JSON columns or compatible schema changes for finding evidence and consensus fields.
- Do not remove legacy `final_review_block` rows; runtime stops using them instead.

### app/backend/alembic/env.py (MODIFY)
- Confirm sys.path uses project root as already fixed for BUG-006.
- Ensure pgvector model imports are included in Alembic metadata discovery.
- Add migration sanity notes so `alembic upgrade head` succeeds on a fresh DB before RAG migration is applied.

### app/backend/services/parser.py (MODIFY)
- Make Docling the primary parser.
- Add OCR fallback path.
- Keep pdfplumber as final fallback behind config.
- Add parsed output structure with page text, clause spans, page numbers, bbox when available, and confidence.
- Add functions: `parse_document()`, `parse_with_docling()`, `parse_with_ocr_fallback()`, `parse_with_pdfplumber_fallback()`, `normalize_clause_evidence()`, `compute_clause_confidence()`.
- Return low-confidence markers consumed by `parser-confidence-contract`.
- Preserve existing parser API compatibility for current run flow.

### app/backend/services/rag_ingestion.py (CREATE)
- Implement RAG ingestion job lifecycle.
- Functions/classes to create: `RagIngestionService`, `create_ingestion_job()`, `ingest_document()`, `parse_source_pdf()`, `chunk_document()`, `compute_file_hash()`, `compute_chunk_hash()`, `embed_chunks()`, `activate_new_version()`, `soft_delete_previous_active_chunks()`, `mark_job_failed()`, `get_ingestion_status()`.
- Inputs: PDF file, `doc_type`, `policy_family_id`, `jurisdiction`, optional `version_label`, auth-derived tenant/workspace.
- Enforce supported `doc_type`: `policy`, `reference_contract`, `playbook`.
- Enforce file size, MIME type, page count, duplicate file hash, duplicate chunk hash, and failed-ingestion rollback.
- Reingestion creates a new version and leaves prior active version intact until new version succeeds.

### app/backend/services/rag_retrieval.py (CREATE)
- Implement Harvey-only retrieval.
- Functions/classes to create: `HarveyRagRetriever`, `retrieve_for_run()`, `retrieve_for_clause()`, `hybrid_search()`, `vector_search()`, `text_search()`, `rerank_results()`, `persist_retrieval_trace()`, `validate_citation_ids()`.
- Filter by tenant/workspace and same `policy_family_id` plus explicitly linked playbooks/reference contracts.
- Return bounded ranked context blocks with trace IDs and chunk IDs.
- Ensure Kira callers cannot use this service.

### app/backend/services/embeddings.py (CREATE)
- Implement embedding provider abstraction for RAG.
- Classes/functions to create: `EmbeddingProvider`, `LocalEmbeddingProvider`, `OpenAICompatibleEmbeddingProvider`, `get_embedding_provider()`, `embed_texts()`, `normalize_embedding()`.
- Read provider/model/dimensions from config.
- Batch embeddings and return deterministic errors for unavailable provider.

### app/backend/services/metrics.py (CREATE)
- Add Prometheus-compatible metrics helpers for `observability-contract`.
- Functions/classes to create: `setup_metrics()`, `record_run_status()`, `record_stage_duration()`, `record_provider_call()`, `record_json_repair()`, `record_retrieval_hit()`, `record_citation_validation_failure()`, `record_queue_age()`, `record_retry()`, `record_worker_lease_expiry()`.
- Expose metrics used by FastAPI and worker.

### app/backend/routes/rag.py (CREATE)
- Create `APIRouter(prefix="/api/rag", tags=["rag"])`.
- Endpoints to create:
- `POST /api/rag/documents`: admin/workspace-admin PDF upload and async ingestion job creation.
- `GET /api/rag/documents`: list RAG documents for auth-derived tenant/workspace.
- `GET /api/rag/documents/{document_id}`: document/version/chunk summary.
- `GET /api/rag/ingestions/{job_id}`: ingestion status.
- `DELETE /api/rag/documents/{document_id}`: soft-delete active document versions/chunks.
- Enforce `auth-derived-tenancy`.
- Return schemas from `app/backend/models/schemas.py`.

### app/backend/routes/contracts.py (MODIFY)
- Keep existing run endpoints; do not rewrite run API.
- Harden `POST /api/runs` so tenant/workspace comes from auth where available instead of trusting form `tenant_id`.
- Keep `GET /api/runs/{id}`, `GET /api/runs/{id}/events`, `GET /api/runs/{id}/events/list`, `POST /api/runs/{id}/human-review`, `POST /api/runs/{id}/retry`, `GET /api/runs/{id}/findings`, `GET /api/runs/{id}/file`.
- Ensure returned findings include `evidence-schema`.
- Ensure retry does not recreate `final_review_block`.
- Add response handling for blocked parser-confidence state.

### app/backend/main.py (MODIFY)
- Include `rag_router`.
- Add `/metrics` endpoint from `app/backend/services/metrics.py`.
- Replace hardcoded CORS with config-driven CORS.
- Ensure existing routers remain mounted.
- Add startup validation for required config: database, provider, embedding provider, RAG settings.

### app/backend/core/config.py (MODIFY)
- Add settings for Docling/OCR/pdfplumber parser fallback.
- Add settings for RAG upload limits, chunk size, chunk overlap, retrieval top_k, rerank top_k, embedding provider, embedding dimensions, and pgvector enabled flag.
- Add settings for model registry path.
- Add settings for metrics enabled flag.
- Add settings for frontend CORS origins.
- Add settings for reviewer temperature and validator temperature.

### app/backend/providers/base.py (MODIFY)
- Ensure provider interface supports system/user messages distinctly.
- Add model endpoint metadata: `name`, `role`, `is_finetuned`, `provider_url`, `enabled`, `timeout_seconds`, `cost_per_1k_tokens`.
- Add structured response contract for strict JSON output.
- Keep async interface consistent.

### app/backend/providers/google_provider.py (MODIFY)
- Implement true system prompt use where supported.
- Ensure async provider methods match `base.py`.
- Add configurable temperature per call.
- Add provider latency/failure metrics from `observability-contract`.

### app/backend/providers/openrouter_provider.py (MODIFY)
- Implement true system prompt use where supported.
- Ensure async provider methods match `base.py`.
- Add configurable temperature per call.
- Add provider latency/failure metrics from `observability-contract`.

### app/backend/services/compliance_repository.py (MODIFY)
- Clarify structured corpus use for Kira as non-vector, non-PDF-RAG.
- Ensure no functions in this repository query `rag_chunks` or `rag_embeddings`.
- Add documentation comments distinguishing structured compliance corpus from Harvey document-vector RAG.

### app/backend/services/policy_repository.py (MODIFY)
- Add helpers for policy family lookup and explicitly linked playbooks/reference contracts.
- Functions to add/modify: `get_policy_family()`, `list_linked_playbooks()`, `list_reference_contracts_for_family()`, `resolve_harvey_rag_scope()`.
- Ensure Harvey retrieval scope uses `harvey-rag-only`.

### app/backend/worker.py (MODIFY)
- Add metrics for queue age, stage duration, retry count, and worker lease expiry.
- Ensure worker never claims removed `final_review_block` for new runs.
- Ensure failed RAG ingestion jobs and failed run stages are logged consistently.

### app/backend/requirements.txt (MODIFY)
- Add Docling dependency.
- Add OCR dependencies selected for the project.
- Add pgvector SQLAlchemy support if needed.
- Add Prometheus client.
- Add MLflow or W&B dependency for eval tracking.
- Add test dependencies: pytest, pytest-asyncio, httpx, respx or equivalent.
- Keep dependency comments noting Docling/OCR image-size impact.

### app/frontend/src/types/index.ts (MODIFY)
- Add TypeScript types matching `evidence-schema`: `ContractEvidence`, `RagCitation`, `RetrievalTraceItem`, `ConsensusState`, `HarveyTrioConsensus`, `RagDocument`, `RagIngestionJob`, `EvalMetricRow`.
- Remove active UI assumptions about `final_review_block`.
- Update `ReviewResult`, `Finding`, `FinalVerdict`, and `StageStatus` to include branch grouping, consensus state, evidence anchors, exploitability, business impact, unresolved_by_consensus, human review fields, retrying, blocked, and failed states.

### app/frontend/src/lib/api.ts (MODIFY)
- Add API functions: `uploadRagDocument()`, `listRagDocuments()`, `getRagDocument()`, `getRagIngestionStatus()`, `deleteRagDocument()`.
- Update run mapping to include `evidence-schema`.
- Ensure polling/SSE helpers do not expect `final_review_block`.
- Preserve existing run functions: create run, get run, retry, start review, submit human review, list findings, file download.
- Add typed handling for parser confidence blocked/warning states.

### app/frontend/src/components/PipelineTracker.tsx (MODIFY)
- Remove `final_review_block` from stage metadata and display order.
- Display the active stages from `stage-topology`.
- Preserve retrying, blocked, failed, and error-detail states.
- Add low-confidence parser warning/blocked display.
- Add Harvey trio state label under Harvey review without adding admin reviewer/final reviewer UI.

### app/frontend/src/components/VerdictCard.tsx (MODIFY)
- Display branch grouping for Harvey trio, Kira, and Admin merged output.
- Display `consensus_state`, `unresolved_by_consensus`, `business_impact`, and `exploitability`.
- Display `contract_evidence` separately from `rag_citations`.
- Show Harvey RAG citations with source path, page, chunk hash, and retrieval trace ID.
- Do not display final/admin reviewer sections.
- Preserve human review action display.

### app/frontend/src/components/HumanReviewPanel.tsx (MODIFY)
- Show Admin consensus output and Harvey trio agreement state.
- Remove final reviewer/admin reviewer state.
- Allow human approve/edit/reject against Admin merged findings.
- Show unresolved consensus warnings requiring human attention.
- Show citation validation warnings and parser confidence warnings.

### app/frontend/src/components/AdminPage.tsx (MODIFY)
- Add admin-only RAG document management area.
- Render `RagDocumentPanel`.
- Ensure only workspace/admin users can access RAG upload controls.
- Do not expose tenant ID text inputs for RAG ingestion.

### app/frontend/src/components/RagDocumentPanel.tsx (CREATE)
- Create admin UI for Harvey RAG source management.
- Components/functions to create: `RagDocumentPanel`, `RagUploadForm`, `RagIngestionStatus`, `RagDocumentTable`, `RagDocumentDetail`.
- Inputs: PDF file, `doc_type`, `policy_family_id`, `jurisdiction`, optional `version_label`.
- Show ingestion status, active version, failed job errors, page/chunk counts, and duplicate detection messages.
- Show that RAG is Harvey-only.

### app/frontend/src/App.tsx (MODIFY)
- Remove assumptions that final reviewer state exists.
- Ensure run display works with updated stage topology.
- Ensure human review entry point appears after Admin merge and `awaiting_human_review`.
- Wire admin page RAG UI if current navigation requires it.
- Preserve upload-to-verdict user flow.

### app/frontend/src/components/AIEngineInsights.tsx (MODIFY)
- Update copy/data model to describe 3 Harvey reviewers, Kira, and Admin merge only.
- Remove references to admin reviewers/final reviewers.
- Add Harvey-only RAG retrieval trace summary if available.

### configs/models.yaml (CREATE)
- Define model registry entries for:
- `gemma_26b_finetuned`: disabled by default until weights/endpoint exist, `is_finetuned: true`.
- `gemma_26b_base`: enabled only when endpoint configured, `is_finetuned: false`.
- `baseline_legal_model_1`: configurable non-fine-tuned baseline.
- `baseline_legal_model_2`: configurable non-fine-tuned baseline.
- Include fields: `name`, `role_support`, `provider_url`, `api_key_env`, `enabled`, `is_finetuned`, `decoding`, `timeout_seconds`, `notes`.
- Missing fine-tuned endpoint must produce `N/A - weights pending` in eval.

### scripts/eval_model_registry.py (CREATE)
- Load and validate `configs/models.yaml`.
- Functions/classes to create: `ModelRegistry`, `ModelEndpoint`, `load_model_registry()`, `validate_model_registry()`, `iter_enabled_models()`, `mark_missing_as_pending()`.
- Verify endpoint availability without failing entire eval on disabled/missing fine-tuned model.

### scripts/eval_finetune_vs_baseline.py (CREATE)
- Run side-by-side evaluation for fine-tuned vs baseline models.
- Functions/classes to create: `EvalRunner`, `run_eval_suite()`, `evaluate_model_on_case()`, `evaluate_role_outputs()`, `evaluate_full_pipeline()`, `write_markdown_report()`, `write_json_results()`.
- Report per-model, per-role, Harvey trio agreement, Kira quality, Admin merge quality, and full-pipeline metrics.
- Use same prompts, retrieval context, decoding params, and fixtures across models.
- Output markdown table even when fine-tuned row is `N/A - weights pending`.

### scripts/eval_metrics.py (CREATE)
- Implement metrics: JSON validity, contradiction precision, contradiction recall, contradiction F1, citation accuracy, hallucinated citation rate, severity calibration, admin agreement, latency, and cost.
- Functions to create: `json_validity_rate()`, `contradiction_prf()`, `citation_accuracy()`, `hallucinated_citation_rate()`, `severity_calibration()`, `admin_agreement_rate()`, `latency_cost_summary()`, `bootstrap_confidence_interval()`, `mcnemar_test()`.

### tests/conftest.py (CREATE)
- Provide test database/session fixtures.
- Provide fake provider fixtures.
- Provide sample uploaded contract fixture.
- Provide sample RAG policy/playbook PDF or extracted-text fixture.
- Provide auth-derived tenant/workspace fixtures.

### tests/test_stage_topology.py (CREATE)
- Test new runs create exactly `stage-topology`.
- Test `final_review_block` is not created for new runs.
- Test SSE events do not emit final/admin reviewer stages.
- Test retry does not recreate removed stages.
- Test legacy final-review rows do not block new finalization.

### tests/test_harvey_trio_consensus.py (CREATE)
- Test exactly 3 Harvey reviewer outputs are produced.
- Test Harvey 1/2/3 agreement calculation.
- Test unresolved Harvey consensus is surfaced to Admin and human review.
- Test Admin merges Harvey consensus with Kira findings without final reviewers.

### tests/test_rag_ingestion.py (CREATE)
- Test PDF upload creates ingestion job.
- Test duplicate file hash handling.
- Test chunk hash dedupe.
- Test reingestion creates new version and preserves prior active version on failure.
- Test old chunks are soft-deleted only after successful new version activation.
- Test tenant/workspace isolation.

### tests/test_rag_retrieval.py (CREATE)
- Test Harvey retrieval filters by tenant, workspace, policy family, jurisdiction, and linked playbooks/reference contracts.
- Test Kira cannot call document-vector RAG.
- Test retrieval trace persistence.
- Test citation IDs validate against retrieved chunk IDs.
- Test citations from another tenant fail validation.

### tests/test_parser_confidence.py (CREATE)
- Test Docling primary parser path.
- Test OCR fallback path.
- Test pdfplumber fallback path.
- Test low-confidence clause warning/blocked behavior.
- Test override path preserves audit information.
- Test confidence appears in contract evidence.

### tests/test_validator_evidence.py (CREATE)
- Test Harvey contradiction without RAG citation is rejected.
- Test Harvey citation not present in retrieval trace is rejected.
- Test Kira finding without contract evidence is rejected.
- Test Kira document-vector RAG citation is rejected.
- Test valid Harvey and Kira findings pass.

### tests/test_run_lifecycle.py (CREATE)
- Integration test for upload to parse to Harvey RAG to Kira to Admin merge to human review to finalized.
- Test retrying/blocked/failed states.
- Test findings endpoint returns evidence and consensus fields.
- Test human approve/edit/reject actions.

### tests/test_eval_metrics.py (CREATE)
- Test metric calculations for JSON validity, contradiction F1, citation accuracy, hallucinated citation rate, severity calibration, bootstrap CI, and McNemar.
- Test disabled fine-tuned endpoint reports `N/A - weights pending`.

### Dockerfile.api (CREATE)
- Containerize FastAPI backend.
- Include system dependencies needed for Docling/OCR/pdfplumber.
- Install backend requirements.
- Run API with production ASGI server.
- Expose configured API port.

### Dockerfile.worker (CREATE)
- Containerize worker process.
- Include same parser/RAG dependencies as API.
- Run `app.backend.worker`.
- Use same environment variable contract as API.

### docker-compose.yml (CREATE)
- Services: api, worker, postgres with pgvector, optional local model/ollama for development, frontend if needed.
- Define volumes for postgres data and model/cache data.
- Define environment variables for database, provider, embeddings, metrics, and CORS.
- Add healthchecks for API, worker dependency, and postgres.
- Do not include production secrets.

### .github/workflows/ci.yml (CREATE)
- Run backend lint/type checks if configured.
- Run frontend build/type check.
- Run unit tests.
- Run integration tests with postgres/pgvector service.
- Do not run GPU eval automatically.
- Upload test logs/artifacts on failure.

### .github/workflows/eval.yml (CREATE)
- Manual dispatch workflow for eval suite.
- Load model registry secrets.
- Run `scripts/eval_finetune_vs_baseline.py`.
- Upload markdown/JSON eval reports.
- Mark missing fine-tuned endpoint as pending, not workflow failure.

### docs/CURRENT_LIMITATIONS.md (CREATE)
- Document what cannot be touched or completed now.
- Categories required: impossible now, blocked by missing model weights, blocked by missing secrets/deployment, deferred by scope, implemented but not benchmark-proven.
- Include no fine-tuned weights currently in repo.
- Include no live production RAG corpus until organization PDFs are uploaded.
- Include deployment/public endpoint pending until secrets/cloud account are configured.
- Include benchmark claims pending eval results.

### docs/CHANGELOG_IMPLEMENTATION.md (CREATE)
- Track every implementation change made by file.
- Include completion extent versus rubric.
- Include remaining gaps after each milestone.
- Include test/eval/deployment status.
- Must be updated alongside implementation changes.

### docs/DEPLOYMENT_RESEARCH.md (CREATE)
- Compare Modal, RunPod Serverless, Hugging Face Inference Endpoints, and one CPU/API host option.
- Verify live sources at implementation time.
- Include VRAM feasibility for target Gemma 26B variant.
- Include cold-start behavior, public endpoint support, secrets strategy, monthly demo estimate, and production estimate.
- Recommended target: Vercel frontend, Render/Fly/Modal CPU FastAPI, Neon/Supabase Postgres+pgvector, Modal vLLM inference unless live-source research disproves it.

### docs/EVALUATION_PLAN.md (CREATE)
- Define fine-tuned vs baseline test suite.
- Define model registry behavior for 1 fine-tuned Gemma 26B plus 3 baselines.
- Define metrics from `scripts/eval_metrics.py`.
- Define per-model, per-role, Harvey trio agreement, Kira quality, Admin merge quality, and full-pipeline reporting.
- Define paired eval, bootstrap CI, and McNemar test.
- State no benchmark superiority claim is allowed until results exist.

### docs/API_SPEC.md (CREATE)
- Document existing run endpoints and new RAG endpoints.
- Include schemas, payload constraints, file limits, auth requirements, tenant derivation, error codes, retry behavior, SSE events, and timeout policy.
- Document removed/disabled final reviewer stage.
- Document evidence schema and citation validation rules.

### docs/ARCHITECTURE.md (CREATE)
- Document service boundaries, API boundaries, Docker services, cloud architecture, and active pipeline diagram.
- Active architecture must show 3 Harvey reviewers, Kira, Admin merge, human review, and no admin reviewers/final reviewers.
- Include Harvey-only RAG flow.
- Include Kira no-document-vector-RAG constraint.
- Include deployment diagram.

### docs/RAG_DESIGN.md (CREATE)
- Document ingestion, parsing, chunking, embeddings, pgvector schema, metadata, retrieval filters, retrieval traces, citation validation, and reingestion/versioning behavior.
- State Harvey-only RAG.
- State Kira cannot query RAG chunks.
- Include duplicate handling and failed ingestion rollback.

### docs/RUNBOOK.md (CREATE)
- Document local setup, migrations, docker compose startup, API/worker startup, frontend startup, RAG ingestion demo, run lifecycle demo, metrics, eval runner, deployment steps, secrets, rollback, and troubleshooting.
- Include blocked/failed/retry handling.
- Include how to interpret `CURRENT_LIMITATIONS.md`.

### app/README.md (MODIFY)
- Remove stale Anthropic references if no longer accurate.
- Update architecture summary to match 3 Harvey reviewers, Kira, Admin merge, human review, Harvey-only RAG, and no admin reviewers.
- Link to `docs/ARCHITECTURE.md`, `docs/API_SPEC.md`, `docs/RAG_DESIGN.md`, `docs/EVALUATION_PLAN.md`, `docs/DEPLOYMENT_RESEARCH.md`, `docs/CURRENT_LIMITATIONS.md`, and `docs/RUNBOOK.md`.
- Add local development and demo readiness instructions.

### AGENTS.md (MODIFY)
- Update project state notes after implementation.
- Record active topology: 3 Harvey reviewers, Kira, Admin merge, no admin reviewers/final reviewers.
- Record Harvey-only RAG and evidence schema rules.
- Record requirement to update README/docs with architecture changes.
</codex_plan>

<output_format>
## Shared Dependencies
`name`: description (used by: file1.py, file2.py)

## Files

### path/to/file.py (CREATE)
- Spec for this new file
- List every function, class, endpoint to create

### path/to/file.py (MODIFY)
- Spec for this existing file
- List every function, class, endpoint to change

</output_format>

<rules>
- Output ONLY the improved plan in the exact structured format above.
- Fix any gaps: missing files, wrong paths, incomplete specs.
- Remove bloat from ## Shared Dependencies — keep dependency names short, descriptions one line max.
- Improve spec bullets under each ### file section: make them specific and actionable.
- Do NOT add unnecessary files. Only include what the task requires.
- Start your output directly with ## Shared Dependencies.
</rules>