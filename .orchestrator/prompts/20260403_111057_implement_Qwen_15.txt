A Claude subagent just implemented one file. Extract memory entries.

<task>[Attached image #1: C:/Users/PinkPanther/Downloads/WhatsApp%20Image%202026-04-03%20at%2010.47.22%20AM.jpeg]
(Use the Read tool to view the attached images above.)

i want you to look at the project, a</task>

<file_spec>
FILE: app/backend/agents/validator.py
SPEC: - Create the branch validator agents.
- Add `HarveyValidatorAgent` and `KiraValidatorAgent`.
- Add functions to normalize reviewer outputs, deduplicate overlapping findings, score evidence quality, and emit a canonical branch decision for admin merge.
</file_spec>

<context>
CLAUDE OUTPUT (what the agent reported doing):
`app/backend/agents/validator.py` is created. Here's what it contains:

**`HarveyValidatorAgent`** — validates 3 Harvey reviewer outputs:
- Calls `provider.generate_structured_output()` with a structured schema asking the LLM to flag hallucinated `clause_uid`s, normalise severity/issue_type, and score each finding
- Strips hallucinated findings, then deduplicates overlapping `(clause_uid, issue_type)` pairs
- Returns `ValidatorOutput(branch="harvey", ...)`

**`KiraValidatorAgent`** — same structure but Kira-aware:
- Passes `jurisdiction` + `regime` into the prompt so the LLM can flag inapplicable regime references
- Populates `inapplicable_regime_flags` in the output

**Three internal helpers** (no extras, just what the spec asked for):
- `_normalize_reviewer_outputs` — flattens 3 × `Branc
</context>

<output_format>
Return a JSON object (omit any key if nothing real to record):
{
  "key_facts": [{"category": "Code Conventions", "fact": "All handlers are async"}],
  "bugs": [{"issue": "...", "root_cause": "...", "solution": "...", "prevention": "..."}],
  "lessons": ["short concrete lesson"]
}
</output_format>

Only record things specific to THIS file and THIS task. No generic advice.
Return ONLY valid JSON, no markdown fences.